node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/

# Bulk sound dump (2026-07-06): ~2000 local-only files from the full pack
# imports — too large for the repo, and music pack 1 has no license file.
# library.json references them; a fresh clone will show them as missing
# (re-run the dump script or use Probe to see the gaps).
campaign-sample/music/mp1-*
campaign-sample/music/fapx-*
campaign-sample/music/noxx-*
campaign-sample/ambience/fapx-*
campaign-sample/ambience/noxx-*
campaign-sample/sfx/fapx-*
campaign-sample/sfx/noxx-*

# Wave 8 music (2026-07-09): Tier-A licensed but 256 MB of MP3s — kept out of
# the repo like the other bulk audio; travels via the campaign zip. The wave-8
# ambience + sfx (31 MB) ARE tracked. library.json references everything.
campaign-sample/music/blacis-*
campaign-sample/music/km-*

# Campaign source-material dumps (large, personal)
/Elor Campaign Import/
# Session recordings (The Chronicler)
campaign-sample/recordings/

# Review campaigns: keep notes + docs, NEVER commit audio or bulk library
campaigns/*/music/
campaigns/*/ambience/
campaigns/*/sfx/
campaigns/*/art/
campaigns/*/recordings/
campaigns/*/library.json
campaigns/*/blocklist.json
